<view class="home-container">
    <header wx:if="{{shop}}" shop="{{shop}}"></header>
    <block wx:if="{{contentList}}" wx:for="{{contentList}}" wx:key="{{index}}">
        <banner wx:if="{{item.type==2}}" bannerData="{{item}}"></banner>
    </block>
</view>