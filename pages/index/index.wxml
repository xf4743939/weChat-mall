<view class="home-container">
    <header wx:if="{{shop}}" shop="{{shop}}"></header>
    <block wx:for="{{Lists}}" wx:key="{{index}}">
        <banner wx:if="{{item.type==2}}" data="{{item}}"></banner>
    </block>
</view>